// objeto Math

let resultado;

resultado = Math.PI;
resultado = Math.round(2.5); // para redondear
resultado = Math.ceil(2.2); //ceil siempre redondea hacia arriba
resultado = Math.floor(2.8); //floor siempre redondea hacia abajo
resultado = Math.sqrt(144); // sacar la raíz cuadrada
resultado = Math.abs(-200); // convertir números negativos en positivos
resultado = Math.min(3, 5, 8, 70, 41, 64, 12, 04); // devuelve el número más chico
resultado = Math.max(3, 5, 8, 70, 41, 64, 12, 04); // devuelve el número más grande
resultado = Math.random(); // genera un número aleatorio
resultado = Math.floor(Math.random() * 2); // generar un 0 o 1


console.log(resultado);
