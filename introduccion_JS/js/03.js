// variables con const

//variables con let

const producto = "Audifonos Gamer"; // inicializamos una variable y le asignamos un valor

let disponible = true; // declaramos una variable sin asignarle un valor.

// producto = true; // reasignando el valor de una variable.

disponible = true;
const producto1 = "computadora",
  disponible1 = true,
  categoria = "Computadoras";

/* var 1disponible; 
var -disponible;  */ //forma incorrecta de declarar variables, no pueden inicializar con un numero o guión medio
const _disponible = true; // se puede declarar una variable que empieza con un guión bajo.

/* Estilos para declarar variables */

const nombre_producto = "Monitor HD"; //underscore
const nombreProducto = "Monitor HD"; //camel case - el más utilizado y el que seguiré usando
const NombreProducto = "Monitor HD"; // Pascal case, lo utilizaremos para las clases
const nombreproducto = "Monitor HD";

console.log(producto);
